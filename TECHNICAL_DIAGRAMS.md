# 🎯 EduPex - Complete Technical Diagram

## System Architecture Overview

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                          EDUPEX SYSTEM ARCHITECTURE                          ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────┐
│         EXTERNAL DEVICE                  │
│      (Any Android Phone)                 │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │    EduPex APK (50-70 MB)           │  │
│  │                                    │  │
│  │  ┌──────────────────────────────┐  │  │
│  │  │  React Frontend              │  │  │
│  │  │  - Login Page                │  │  │
│  │  │  - Dashboard                 │  │  │
│  │  │  - Lessons & Quizzes         │  │  │
│  │  │  - Achievements              │  │  │
│  │  │  - Progress Tracking         │  │  │
│  │  │  - Hardcoded credentials:    │  │  │
│  │  │    • test@edupex.com         │  │  │
│  │  │    • test123 (password)      │  │  │
│  │  └──────────────┬───────────────┘  │  │
│  │                 │                   │  │
│  │                 │ API Requests      │  │
│  │                 │ (with JWT token)  │  │
│  │                 │                   │  │
│  │  ┌──────────────▼───────────────┐  │  │
│  │  │  API Client (Axios)          │  │  │
│  │  │  - Interceptors              │  │  │
│  │  │  - Token management          │  │  │
│  │  │  - Error handling            │  │  │
│  │  └──────────────┬───────────────┘  │  │
│  │                 │                   │  │
│  │  ┌──────────────▼───────────────┐  │  │
│  │  │  localStorage                │  │  │
│  │  │  - JWT token                 │  │  │
│  │  │  - User data                 │  │  │
│  │  │  - Preferences               │  │  │
│  │  └──────────────────────────────┘  │  │
│  └────────────────────────────────────┘  │
│                     │                     │
│                     │ Internet/HTTPS      │
│                     │                     │
└─────────────────────┼─────────────────────┘
                      │
                      ▼
      ╔══════════════════════════════════╗
      ║  RENDER.COM (Deployed Backend)   ║
      ║  https://edupex-backend.on...    ║
      ║  /api                            ║
      ║                                  ║
      ║  ┌────────────────────────────┐  ║
      ║  │  Express.js Server         │  ║
      ║  │  Port: 5000 (on Render)    │  ║
      ║  │                            │  ║
      ║  │  Routes:                   │  ║
      ║  │  - /api/users/login ✓      │  ║
      ║  │  - /api/users/register ✓   │  ║
      ║  │  - /api/lessons ✓          │  ║
      ║  │  - /api/progress ✓         │  ║
      ║  │  - /api/assistant ✓        │  ║
      ║  └────────────────────────────┘  ║
      ║             │                     ║
      ║             │ Mongoose ODM        ║
      ║             │                     ║
      ║  ┌──────────▼───────────────────┐ ║
      ║  │  MongoDB Database            │ ║
      ║  │                              │ ║
      ║  │  Collections:                │ ║
      ║  │  ├─ users                    │ ║
      ║  │  │  ├─ test@edupex.com ✓    │ ║
      ║  │  │  ├─ password (bcrypt)     │ ║
      ║  │  │  └─ profile data          │ ║
      ║  │  ├─ lessons                  │ ║
      ║  │  │  ├─ Math (grades 5-8)     │ ║
      ║  │  │  └─ Romanian (grades 5-8) │ ║
      ║  │  ├─ progress                 │ ║
      ║  │  │  └─ user progress tracks  │ ║
      ║  │  ├─ achievements             │ ║
      ║  │  │  └─ badges & milestones   │ ║
      ║  │  └─ other models             │ ║
      ║  └──────────────────────────────┘ ║
      ╚══════════════════════════════════╝
```

---

## User Login Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  USER OPENS APK                                                 │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  CAPACITOR DETECTS: Android Platform                            │
│  API URL: https://edupex-backend.onrender.com/api              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  LOGIN PAGE DISPLAYS                                            │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ Email input field                                       │  │
│  │ Password input field                                    │  │
│  │ [Autentificare]  button                                 │  │
│  │ [🎓 Intră cu Cont Demo]  button ← HARDCODED             │  │
│  │ Don't have account? Register link                       │  │
│  └─────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
        ┌──────────────┐       ┌──────────────┐
        │ Click Demo   │       │ Manual Entry │
        │ Button       │       │ & Submit     │
        └──────┬───────┘       └──────┬───────┘
               │                      │
               └──────────┬───────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND: Auto-fill credentials                                │
│  • email = "test@edupex.com"                                   │
│  • password = "test123"                                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND: POST REQUEST                                         │
│  URL: https://edupex-backend.onrender.com/api/users/login      │
│  Headers: {                                                     │
│    "Content-Type": "application/json"                           │
│  }                                                              │
│  Body: {                                                        │
│    "email": "test@edupex.com",                                 │
│    "password": "test123"                                        │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ Internet (HTTPS)
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND: Receive login request                                 │
│  Express route: POST /api/users/login                           │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND: Query MongoDB                                         │
│  await User.findOne({ email: "test@edupex.com" })             │
│  Result: User object found ✓                                   │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND: Verify password                                       │
│  await user.isValidPassword("test123")                          │
│  Using bcrypt.compare() against hashed password                 │
│  Result: Password matches ✓                                    │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND: Generate JWT Token                                    │
│  jwt.sign({                                                     │
│    userId: "507f1f77bcf86cd799439011"                          │
│  }, process.env.JWT_SECRET, {                                  │
│    expiresIn: "7d"                                              │
│  })                                                             │
│  Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...               │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  BACKEND: Return response                                       │
│  {                                                              │
│    "message": "Login successful",                               │
│    "token": "eyJhbGc...",                                      │
│    "user": {                                                    │
│      "id": "507f1f77bcf86cd799439011",                         │
│      "username": "testedupex",                                  │
│      "firstName": "Test",                                       │
│      "lastName": "User",                                        │
│      "email": "test@edupex.com",                               │
│      "gradeLevel": 5,                                           │
│      "xpPoints": 0,                                             │
│      "level": 1,                                                │
│      "streak": 0,                                               │
│      "hearts": 5                                                │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │ HTTPS Response
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND: Receive response                                     │
│  Status: 200 OK ✓                                              │
│  Data: token + user object                                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND: Store in localStorage                                │
│  localStorage.setItem('token', 'eyJhbGc...')                   │
│  localStorage.setItem('user', JSON.stringify(userData))         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND: Update Redux state                                   │
│  dispatch({                                                     │
│    type: 'USER_LOGIN_SUCCESS',                                  │
│    payload: { user, token }                                     │
│  })                                                             │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  FRONTEND: Redirect to Dashboard                                │
│  navigate('/dashboard')                                         │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  ✅ USER IS LOGGED IN                                           │
│  Dashboard loads with user stats:                               │
│  • XP: 0                                                        │
│  • Level: 1                                                     │
│  • Streak: 0                                                    │
│  • Hearts: 5                                                    │
│                                                                 │
│  Can now:                                                       │
│  ✓ View Math lessons                                            │
│  ✓ View Romanian lessons                                        │
│  ✓ Take quizzes                                                 │
│  ✓ Earn XP and level up                                         │
│  ✓ Unlock achievements                                          │
│  ✓ Track progress                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## File Structure & What's Hardcoded

```
EduPex Project
│
├── 🔐 HARDCODED ELEMENTS
│   └── frontend/src/pages/Login.js
│       ├── demoEmail = 'test@edupex.com'      ← HARDCODED
│       ├── demoPassword = 'test123'            ← HARDCODED
│       └── handleDemoLogin() function          ← HARDCODED
│
├── 🔗 BACKEND URL CONFIGURATION
│   └── frontend/src/config/apiConfig.js
│       └── if (NODE_ENV === 'production')
│           return 'https://edupex-backend.onrender.com/api'  ← HARDCODED
│
├── 🗄️ TEST USER IN DATABASE
│   ├── backend/checkTestUser.js
│   │   └── Creates test@edupex.com with password test123
│   │
│   └── MongoDB
│       └── users collection
│           └── {
│               email: "test@edupex.com",
│               password: "$2a$10$..." (bcrypt hashed),
│               username: "testedupex",
│               firstName: "Test",
│               lastName: "User",
│               gradeLevel: 5,
│               ...
│           }
│
└── 📱 FINAL APK
    └── frontend/android/app/build/outputs/apk/debug/app-debug.apk
        ├── Contains hardcoded credentials
        ├── Contains production backend URL
        ├── Contains all React code
        ├── Contains Capacitor Android wrapper
        └── Size: ~50-70 MB
```

---

## API Call Sequence (With Token)

```
After login, all API calls include token:

┌─────────────────────────────┐
│ Frontend Makes Request:     │
│                             │
│ GET /api/lessons            │
│                             │
│ Headers: {                  │
│   "Authorization":          │
│   "Bearer eyJhbGc..."       │ ← Token from login
│ }                           │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ Backend Middleware:         │
│                             │
│ authMiddleware() {          │
│   token = request.header()  │
│   verify token              │
│   find user by userId       │
│   attach user to request    │
│   next()                    │
│ }                           │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│ API Route Handler:          │
│                             │
│ Route receives:             │
│ - req.user (authenticated)  │
│ - req.token (valid)         │
│                             │
│ Can now access user data    │
│ and return appropriate data │
└─────────────────────────────┘
```

---

## Gamification System Architecture

```
User Progress Tracking:

┌─────────────────────────────────────┐
│        USER ACTIONS                 │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Complete a Lesson: +10 XP         │
│   Complete a Quiz: +25 XP           │
│   Unlock Achievement: +5 XP         │
│   Daily Streak: 1.5x XP multiplier  │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   XP Accumulation:                  │
│   0-100 = Level 1                   │
│   100-250 = Level 2                 │
│   250-450 = Level 3                 │
│   ... and so on                     │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Streak Tracking:                  │
│   Daily login = +1 streak           │
│   Miss day = reset to 0             │
│   Special badge at 7, 30, 100 days  │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Hearts System:                    │
│   Start: 5 hearts                   │
│   Wrong answer: -1 heart            │
│   Daily reset: +5 hearts            │
│   Premium feature: restore faster   │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Achievements Unlocked:            │
│   First lesson complete             │
│   Level 5 reached                   │
│   7-day streak                      │
│   100 XP earned                     │
│   Math mastery (90% accuracy)       │
│   Romanian mastery (90% accuracy)   │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│   Updated in Database:              │
│   users.xpPoints                    │
│   users.level                       │
│   users.streak                      │
│   users.hearts                      │
│   users.achievements[]              │
│                                     │
│   progress.lessonId                 │
│   progress.completed                │
│   progress.score                    │
│   progress.completedAt              │
└─────────────────────────────────────┘
```

---

## Build & Deployment Pipeline

```
┌────────────────────────────────────┐
│  1. DEVELOP & TEST                 │
│     npm start (local dev)           │
│     Test features locally           │
└────────────────────────────────────┘
                  │
                  ▼
┌────────────────────────────────────┐
│  2. BUILD REACT APP                │
│     npm run build                  │
│     Output: /frontend/build/        │
│     - Minified JS/CSS               │
│     - Optimized for production      │
└────────────────────────────────────┘
                  │
                  ▼
┌────────────────────────────────────┐
│  3. SYNC WITH CAPACITOR            │
│     npx cap sync android           │
│     - Copy built files to android/  │
│     - Update capacitor config      │
└────────────────────────────────────┘
                  │
                  ▼
┌────────────────────────────────────┐
│  4. BUILD APK WITH GRADLE          │
│     cd android                     │
│     ./gradlew assembleDebug        │
│     Output: app-debug.apk          │
│     - Contains built React code    │
│     - Android native wrapper       │
│     - All dependencies             │
│     - Assets & resources           │
└────────────────────────────────────┘
                  │
                  ▼
┌────────────────────────────────────┐
│  5. APK READY FOR DISTRIBUTION     │
│     /frontend/android/app/build/   │
│     outputs/apk/debug/             │
│     app-debug.apk (50-70 MB)       │
└────────────────────────────────────┘
                  │
                  ▼
┌────────────────────────────────────┐
│  6. INSTALL ON DEVICE              │
│     Option A: adb install          │
│     Option B: Email/Cloud          │
│     Option C: Manual installation  │
└────────────────────────────────────┘
                  │
                  ▼
┌────────────────────────────────────┐
│  7. USER LAUNCHES APP              │
│     APK extracts on device         │
│     React Native bridge initializes│
│     App starts with hardcoded URL  │
│     Login page displays            │
└────────────────────────────────────┘
```

---

## Database Schema Overview

```
MongoDB Collections:

USERS Collection:
{
  _id: ObjectId,
  username: String (unique),
  email: String (unique, "test@edupex.com"),
  password: String (bcrypt hashed, "test123"),
  firstName: String ("Test"),
  lastName: String ("User"),
  gradeLevel: Number (5, 6, 7, or 8),
  xpPoints: Number (0-∞),
  level: Number (1-∞),
  streak: Number (0-∞),
  hearts: Number (0-5),
  achievements: [ObjectId] (refs to Achievement),
  preferences: {
    aiTeacherGender: "female" or "male",
    notificationsEnabled: Boolean,
    dailyGoal: Number
  },
  createdAt: Date,
  lastActive: Date
}

LESSONS Collection:
{
  _id: ObjectId,
  title: String,
  description: String,
  subject: String ("Math" or "Romanian"),
  gradeLevel: Number (5-8),
  content: String or Array,
  questions: Array of Question objects,
  videoUrl: String,
  difficulty: String
}

PROGRESS Collection:
{
  _id: ObjectId,
  userId: ObjectId (ref to User),
  lessonId: ObjectId (ref to Lesson),
  completed: Boolean,
  score: Number (0-100),
  attemptCount: Number,
  completedAt: Date
}

ACHIEVEMENTS Collection:
{
  _id: ObjectId,
  userId: ObjectId (ref to User),
  badgeId: String,
  type: String ("streak", "level", "quiz"),
  unlockedAt: Date
}
```

---

This diagram shows the complete technical architecture of how EduPex works!

